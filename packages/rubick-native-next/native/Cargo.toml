[package]
name = "rubick_native_next"
version = "0.0.1"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
# napi6 enables the threadsafe-function & async-task ABI we rely on.
napi = { version = "2", default-features = false, features = ["napi6"] }
napi-derive = "2"
windows = { version = "0.59", features = [
  "Win32_Foundation",
  "Win32_System_Com",
  "Win32_System_DataExchange",
  "Win32_System_LibraryLoader",
  "Win32_System_Memory",
  "Win32_System_Ole",
  "Win32_System_Threading",
  "Win32_System_Variant",
  "Win32_UI_Input_KeyboardAndMouse",
  "Win32_UI_WindowsAndMessaging",
  "Win32_UI_Shell",
] }

[build-dependencies]
napi-build = "2"

# This crate is shipped as a `.node` addon loaded by Electron. Keep release
# binaries small and start-up fast; LTO + single codegen unit pays off here.
[profile.release]
lto = "thin"
codegen-units = 1
strip = true
panic = "abort"
opt-level = 3
